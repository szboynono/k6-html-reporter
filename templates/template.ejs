<!DOCTYPE html>
<html>

<head>
  <title>K6 Summary Report</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
  <style>
    ::-webkit-scrollbar {
      width: 0px;
      background: transparent; /* make scrollbar transparent */
    }
    .fail-block {
      background-color: #F9DCDE;
      color: #9b0909;
    }
    .success-block {
      background-color: #D7EADA;
      color: #283618;
    }
    .banner-danger {
      background-color: #D94851;
      color: white;
    }
    .banner-success {
      background-color: #D7EADA;
      color: #283618;
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="jumbotron jumbotron-fluid bg-white my-5">
      <div class="container px-0">
        <h1 class="display-3 text-primary">K6 Summary Report</h1>
        <p class="text-secondary">Powered by k6-html-reporter</p>
      </div>

      <% if(allThresholds.length > 0) { %>
        <div class="my-3">
          <div class="mt-3">
            <button class="btn btn-lg fs-2 px-0 text-primary" data-bs-toggle="collapse" data-bs-target="#collapseThreshold"
            aria-expanded="false" aria-controls="collapseThreshold">
              Thresholds
            </button>
            <p class="text-secondary">Pass/fail criteria used to specify the performance expectations of the system under test.</p>
            <% if(totalThresholdResult.fails > 0) { %>
              <div class="alert banner-danger mt-3" role="alert">
                <%=totalThresholdResult.fails%> thresholds were not met
              </div>
            <% } else { %>
              <div class="alert banner-success my-2" role="alert">
                All thresholds were met!
              </div>
            <% } %>
          </div>
          
          <div class="collapse show" id="collapseThreshold">
            <p class="d-inline-block me-3 text-success">passes: <%= totalThresholdResult.passes %></p>
            <p class="d-inline-block me-3 text-danger">fails: <%= totalThresholdResult.fails %></p>
            <ul class="list-group">
              <%allThresholds.forEach(data => {%>
                <li class="list-group-item">
                  <h6><%= data.name %></h6>
                  <ul class="list-group">
                    <% for(let [key, value] of Object.entries(data.thresholds)) {%>
                    <% if(value.ok === false) { %>
                    <li class="list-group-item fail-block">
                    <%} else {%>
                    <li class="list-group-item success-block">
                    <%}%>
                      <%= key %>
                    </li>
                    <%}%>
                  </ul>
                </li>
              <%})%>
            </ul>
          </div>
        </div>
      <% } %>

      <% if(checks.length > 0) {%>
      <div>
        <div>
          <button class="btn btn-lg fs-2 px-0 text-primary" data-bs-toggle="collapse" data-bs-target="#collapseExample"
            aria-expanded="false" aria-controls="collapseExample">
            Checks
          </button>
          <p class="text-secondary">Asserts that don't halt the execution unless specified in the threshold.</p>
        </div>
        <div class="collapse show mt-4" id="collapseExample">
          <table class="table table-hover table-sm">
            <thead>
              <tr>
                <th scope="col">#</th>
                <th scope="col">check</th>
                <th scope="col">passes</th>
                <th scope="col">fails</th>
              </tr>
            </thead>
            <tbody>
              <% for(var i=0; i < checks.length; i++) { %>
              <% if(checks[i].fails > 0) { %>
              <tr class="fail-block">
                <% } else { %>
              <tr>
                <% } %>
                <th scope="row" class="text-secondary"><%= i+1 %></th>
                <td><%= checks[i].pathArray%></td>
                <td><%= checks[i].passes %></td>
                <td><%= checks[i].fails %></td>
              </tr>
              <% } %>
            </tbody>
          </table>
          <p class="d-inline-block me-3 text-success">passes: <%= checkMetric.values.passes %></p>
          <p class="d-inline-block me-3 text-danger">fails: <%= checkMetric.values.fails %></p>
          <p class="d-inline-block text-info">pass rate: <%= (checkMetric.values.passes * 100/(checkMetric.values.fails + checkMetric.values.passes)).toFixed(2) %>%</p>
          
          <% if(checkMetric["thresholds"]) { %>
            <% if(checkMetric.thresholdFailed === true) { %>
              <p class="text-danger">Some thresholds were not met, more details in the threshold session</p>
            <% } else { %>
              <p class="text-success">All thresholds for checks were met</p>
            <% } %>
          <% } %>
        </div>
      </div>
      <% } %>

      <div class="mt-3">
        <div>
          <button class="btn btn-lg fs-2 px-0 text-primary" data-bs-toggle="collapse" data-bs-target="#collapseMetric"
            aria-expanded="false" aria-controls="collapseMetric">
            Metrics
          </button>
          <p class="text-secondary">Important aspect of metrics management in k6</p>
        </div>
        <div class="collapse show" id="collapseMetric">
          <% if(countMetrics.length > 0) {%>
          <div class="mb-3">
            <h5>Measured based on rate</h5>
            <table class="table table-hover table-sm">
              <thead>
                <tr>
                  <th scope="col">#</th>
                  <th scope="col">name</th>
                  <th scope="col">count</th>
                  <th scope="col">rate</th>
                </tr>
              </thead>
              <tbody>
                <% for(let i=0; i < countMetrics.length; i++) { %>
                <% if(countMetrics[i].thresholdFailed === true) {%>
                <tr class="fail-block">
                <% } else { %>
                <tr>
                <% } %>
                  <th scope="row" class="text-secondary"><%= i+1 %></th>
                  <td><%= countMetrics[i].name %></td>
                  <td><%= countMetrics[i].values.count %></td>
                  <td><%= countMetrics[i].values.rate %></td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
          <% } %>

          <% if(timeMetrics.length > 0) {%>
          <div class="mb-3">
            <h5>Measured based on time</h5>
            <table class="table table-hover table-sm">
              <thead>
                <tr>
                  <th scope="col">#</th>
                  <th scope="col">name</th>
                  <th scope="col">avg</th>
                  <th scope="col">max</th>
                  <th scope="col">med</th>
                  <th scope="col">min</th>
                  <th scope="col">P(90)</th>
                  <th scope="col">P(95)</th>
                </tr>
              </thead>
              <tbody>
                <% for(let i=0; i < timeMetrics.length; i++) { %>
                <% if(timeMetrics[i].thresholdFailed === true) {%>
                <tr class="fail-block">
                <% } else { %>
                <tr>
                <% } %>
                  <th scope="row" class="text-secondary"><%= i+1 %></th>
                  <td><%= timeMetrics[i].name %></td>
                  <td><%= timeMetrics[i].values.avg.toFixed(2) %> ms</td>
                  <td><%= timeMetrics[i].values.max.toFixed(2) %> ms</td>
                  <td><%= timeMetrics[i].values.med.toFixed(2) %> ms</td>
                  <td><%= timeMetrics[i].values.min.toFixed(2) %> ms</td>
                  <td><%= timeMetrics[i].values["p(90)"].toFixed(2) %> ms</td>
                  <td><%= timeMetrics[i].values["p(95)"].toFixed(2) %> ms</td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
          <% } %>

          <% if(vusMetrics.length > 0) {%>
            <div>
              <h5>Measured based on vus</h5>
              <table class="table table-hover table-sm">
                <thead>
                  <tr>
                    <th scope="col">#</th>
                    <th scope="col">name</th>
                    <th scope="col">max</th>
                    <th scope="col">min</th>
                    <th scope="col">value</th>
                  </tr>
                </thead>
                <tbody>
                  <% for(let i=0; i < vusMetrics.length; i++) { %>
                  <% if(vusMetrics[i].thresholdFailed === true) {%>
                  <tr class="fail-block">
                  <% } else { %>
                  <tr>
                  <% } %>
                    <th scope="row" class="text-secondary"><%= i+1 %></th>
                    <td><%= vusMetrics[i].name %></td>
                    <td><%= vusMetrics[i].values.max %></td>
                    <td><%= vusMetrics[i].values.min %></td>
                    <td><%= vusMetrics[i].values.value %></td>
                  </tr>
                  <% } %>
                </tbody>
              </table>
              <% if(allThresholds.length > 0) { %>
                <%if(metricThresholdsPassed === true) {%>
                  <p class="text-success mt-4">All thresholds for metrics were met</p>
                <%} else {%>
                  <p class="text-danger"><%=totalThresholdResult.failedMetricsNum%> thresholds were not met, more details in the threshold session</p>
                <%}%>
              <% } %>
            </div>
          <% } %>
        </div>
      </div>





    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-ygbV9kiqUc6oa4msXn9868pTtWMgiQaeYH7/t7LECLbyPA2x65Kgf80OJFdroafW"
    crossorigin="anonymous"></script>
</body>

</html>